# Optional: live geocoding (week 2). Offline CSV fallback works without this.
# GEOCODER=nominatim

# Optional: live hazard APIs (week 3). Bundled sample grids work offline.
# HAZARD_MODE=offline

# Active client track: ironwood | strata | northwood
CLIENT_TRACK=ironwood

# Optional xAI / other keys if you extend the course
# XAI_API_KEY=
